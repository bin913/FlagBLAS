# Copyright 2026 FlagOS Contributors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

ops:
  - id: sgemm
    description: |
      single-precision GEMM, it uses standard FP32 floating point.
      This is a simple wrapper of the existing BLAS `gemm` operator.
    for:
      - sgemm
    labels:
      - sgemm
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: dgemm
    description: |
      Double-precision GEMM, based on FP64 format.
      This is a simple wrapper of the existing BLAS float64 `gemm` operator.
    for:
      - dgemm
    labels:
      - dgemm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: cgemm
    description: |
      Single-precision complex GEMM, based on complex64 format.
      This is a simple wrapper of the existing BLAS complex64 `gemm` operator.
    for:
      - cgemm
    labels:
      - cgemm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zgemm
    description: |
      Double-precision complex GEMM, based on complex128 format.
      This is a simple wrapper of the existing BLAS complex128 `gemm` operator.
    for:
      - zgemm
    labels:
      - zgemm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: hgemm
    description: |
      half-precision GEMM, based on FP16 format.
      This is a simple wrapper of the existing BLAS float16 `gemm` operator.
    for:
      - hgemm
    labels:
      - hgemm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: bfgemm
    description: |
      Refers to GEMM with BF16 data type.
      This is a simple wrapper of the existing BLAS bfloat16`gemm` operator.
    for:
      - bfgemm
    labels:
      - bfgemm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: fp8gemm
    description: |
      8-bit floating point GEMM with E4M3/E5M2 common formats
      This is a simple wrapper of the existing torch `abs` operator.
    for:
      - fp8gemm
    labels:
      - gemm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: group_bfgemm
    description: |
      Refers to GROUP_GEMM with BF16 data type.
      This is a simple wrapper of the existing BLAS bfloat16 `group_gemm` operator.
    for:
      - group_bfgemm
    labels:
      - gemm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: group_hgemm
    description: |
      Refers to GROUP_GEMM with FP16 data type.
      This is a simple wrapper of the existing BLAS float16 `group_gemm` operator.
    for:
      - group_hgemm
    labels:
      - gemm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: group_tf32gemm
    description: |
      Refers to GROUP_GEMM with TF32 data type.
      This is a simple wrapper of the existing BLAS tf32 `group_gemm` operator.
    for:
      - group_tf32gemm
    labels:
      - gemm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: sabs
    description: |
      Single-precision BLAS Level 1 absolute-value operation.
      Computes y[i] = abs(x[i]) for a contiguous float32 vector.
    for:
      - sabs
    labels:
      - sabs
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dabs
    description: |
      Double-precision BLAS Level 1 absolute-value operation.
      Computes y[i] = abs(x[i]) for a contiguous float64 vector.
    for:
      - dabs
    labels:
      - dabs
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: cabs
    description: |
      Single-precision complex BLAS Level 1 absolute-value operation.
      Computes the magnitude of each complex64 input element into a float32 output vector.
    for:
      - cabs
    labels:
      - cabs
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zabs
    description: |
      Double-precision complex BLAS Level 1 absolute-value operation.
      Computes the magnitude of each complex128 input element into a float64 output vector.
    for:
      - zabs
    labels:
      - abs
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: samax
    description: |
      Single-precision BLAS Level 1 index-of-maximum-absolute-value reduction.
      Returns the 1-based index of the float32 vector element with the largest absolute value.
    for:
      - samax
    labels:
      - amax
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: damax
    description: |
      Double-precision BLAS Level 1 index-of-maximum-absolute-value reduction.
      Returns the 1-based index of the float64 vector element with the largest absolute value.
    for:
      - damax
    labels:
      - amax
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: camax
    description: |
      Single-precision complex BLAS Level 1 index-of-maximum-absolute-value reduction.
      Returns the 1-based index of the complex64 element with the largest BLAS complex absolute value.
    for:
      - camax
    labels:
      - amax
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zamax
    description: |
      Double-precision complex BLAS Level 1 index-of-maximum-absolute-value reduction.
      Returns the 1-based index of the complex128 element with the largest BLAS complex absolute value.
    for:
      - zamax
    labels:
      - amax
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: samin
    description: |
      Single-precision BLAS Level 1 index-of-minimum-absolute-value reduction.
      Returns the 1-based index of the float32 vector element with the smallest absolute value.
    for:
      - samin
    labels:
      - amin
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: damin
    description: |
      Double-precision BLAS Level 1 index-of-minimum-absolute-value reduction.
      Returns the 1-based index of the float64 vector element with the smallest absolute value.
    for:
      - damin
    labels:
      - amin
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: camin
    description: |
      Single-precision complex BLAS Level 1 index-of-minimum-absolute-value reduction.
      Returns the 1-based index of the complex64 element with the smallest BLAS complex absolute value.
    for:
      - camin
    labels:
      - amin
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zamin
    description: |
      Double-precision complex BLAS Level 1 index-of-minimum-absolute-value reduction.
      Returns the 1-based index of the complex128 element with the smallest BLAS complex absolute value.
    for:
      - zamin
    labels:
      - amin
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: sasum
    description: |
      Single-precision BLAS Level 1 absolute-sum reduction.
      Computes the sum of absolute values for a strided float32 vector.
    for:
      - sasum
    labels:
      - asum
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dasum
    description: |
      Double-precision BLAS Level 1 absolute-sum reduction.
      Computes the sum of absolute values for a strided float64 vector.
    for:
      - dasum
    labels:
      - asum
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: scasum
    description: |
      Single-precision complex BLAS Level 1 absolute-sum reduction.
      Computes the sum of abs(real(x[i])) + abs(imag(x[i])) for a strided complex64 vector.
    for:
      - scasum
    labels:
      - asum
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dzasum
    description: |
      Double-precision complex BLAS Level 1 absolute-sum reduction.
      Computes the sum of abs(real(x[i])) + abs(imag(x[i])) for a strided complex128 vector.
    for:
      - dzasum
    labels:
      - asum
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: saxpy
    description: |
      Single-precision BLAS Level 1 scaled vector addition.
      Updates y in place with y = alpha * x + y for strided float32 vectors.
    for:
      - saxpy
    labels:
      - axpy
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: daxpy
    description: |
      Double-precision BLAS Level 1 scaled vector addition.
      Updates y in place with y = alpha * x + y for strided float64 vectors.
    for:
      - daxpy
    labels:
      - axpy
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: caxpy
    description: |
      Single-precision complex BLAS Level 1 scaled vector addition.
      Updates y in place with y = alpha * x + y for strided complex64 vectors.
    for:
      - caxpy
    labels:
      - axpy
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zaxpy
    description: |
      Double-precision complex BLAS Level 1 scaled vector addition.
      Updates y in place with y = alpha * x + y for strided complex128 vectors.
    for:
      - zaxpy
    labels:
      - axpy
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: scopy
    description: |
      Single-precision BLAS Level 1 vector copy.
      Copies a strided float32 source vector x into a strided destination vector y.
    for:
      - scopy
    labels:
      - copy
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dcopy
    description: |
      Double-precision BLAS Level 1 vector copy.
      Copies a strided float64 source vector x into a strided destination vector y.
    for:
      - dcopy
    labels:
      - copy
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: ccopy
    description: |
      Single-precision complex BLAS Level 1 vector copy.
      Copies a strided complex64 source vector x into a strided destination vector y.
    for:
      - ccopy
    labels:
      - copy
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zcopy
    description: |
      Double-precision complex BLAS Level 1 vector copy.
      Copies a strided complex128 source vector x into a strided destination vector y.
    for:
      - zcopy
    labels:
      - copy
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: sdot
    description: |
      Single-precision BLAS Level 1 dot-product reduction.
      Computes the dot product of strided float32 vectors x and y into a scalar result.
    for:
      - sdot
    labels:
      - dot
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: ddot
    description: |
      Double-precision BLAS Level 1 dot-product reduction.
      Computes the dot product of strided float64 vectors x and y into a scalar result.
    for:
      - ddot
    labels:
      - dot
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: cdotu
    description: |
      Single-precision complex BLAS Level 1 unconjugated dot-product reduction.
      Computes the unconjugated dot product of strided complex64 vectors x and y.
    for:
      - cdotu
    labels:
      - dotu
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zdotu
    description: |
      Double-precision complex BLAS Level 1 unconjugated dot-product reduction.
      Computes the unconjugated dot product of strided complex128 vectors x and y.
    for:
      - zdotu
    labels:
      - dotu
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: cdotc
    description: |
      Single-precision complex BLAS Level 1 conjugated dot-product reduction.
      Computes the conjugated dot product of strided complex64 vectors x and y.
    for:
      - cdotc
    labels:
      - dotc
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zdotc
    description: |
      Double-precision complex BLAS Level 1 conjugated dot-product reduction.
      Computes the conjugated dot product of strided complex128 vectors x and y.
    for:
      - zdotc
    labels:
      - dotc
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: sswap
    description: |
      Single-precision BLAS Level 1 vector swap.
      Swaps elements between strided float32 vectors x and y in place.
    for:
      - sswap
    labels:
      - swap
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dswap
    description: |
      Double-precision BLAS Level 1 vector swap.
      Swaps elements between strided float64 vectors x and y in place.
    for:
      - dswap
    labels:
      - swap
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: cswap
    description: |
      Single-precision complex BLAS Level 1 vector swap.
      Swaps elements between strided complex64 vectors x and y in place.
    for:
      - cswap
    labels:
      - swap
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zswap
    description: |
      Double-precision complex BLAS Level 1 vector swap.
      Swaps elements between strided complex128 vectors x and y in place.
    for:
      - zswap
    labels:
      - swap
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: snrm2
    description: |
      Single-precision BLAS Level 1 Euclidean norm reduction.
      Computes sqrt(sum(x[i] ** 2)) for a strided float32 vector.
    for:
      - snrm2
    labels:
      - nrm2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dnrm2
    description: |
      Double-precision BLAS Level 1 Euclidean norm reduction.
      Computes sqrt(sum(x[i] ** 2)) for a strided float64 vector.
    for:
      - dnrm2
    labels:
      - nrm2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: scnrm2
    description: |
      Single-precision complex BLAS Level 1 Euclidean norm reduction.
      Computes sqrt(sum(abs(x[i]) ** 2)) for a strided complex64 vector.
    for:
      - scnrm2
    labels:
      - nrm2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dznrm2
    description: |
      Double-precision complex BLAS Level 1 Euclidean norm reduction.
      Computes sqrt(sum(abs(x[i]) ** 2)) for a strided complex128 vector.
    for:
      - dznrm2
    labels:
      - nrm2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: srot
    description: |
      Single-precision BLAS Level 1 plane rotation.
      Applies a Givens rotation to strided float32 vectors x and y in place.
    for:
      - srot
    labels:
      - rot
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: drot
    description: |
      Double-precision BLAS Level 1 plane rotation.
      Applies a Givens rotation to strided float64 vectors x and y in place.
    for:
      - drot
    labels:
      - rot
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: crot
    description: |
      Single-precision complex BLAS Level 1 plane rotation.
      Applies a Givens rotation to strided complex64 vectors x and y in place.
    for:
      - crot
    labels:
      - rot
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zrot
    description: |
      Double-precision complex BLAS Level 1 plane rotation.
      Applies a Givens rotation to strided complex128 vectors x and y in place.
    for:
      - zrot
    labels:
      - rot
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: srotg
    description: |
      Single-precision BLAS Level 1 Givens rotation parameter generation.
      Updates scalar float32 inputs a and b and writes float32 c and s.
    for:
      - srotg
    labels:
      - rotg
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: drotg
    description: |
      Double-precision BLAS Level 1 Givens rotation parameter generation.
      Updates scalar float64 inputs a and b and writes float64 c and s.
    for:
      - drotg
    labels:
      - rotg
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: crotg
    description: |
      Single-precision complex BLAS Level 1 Givens rotation parameter generation.
      Updates scalar complex64 input a and writes real float32 c plus complex64 s.
    for:
      - crotg
    labels:
      - rotg
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zrotg
    description: |
      Double-precision complex BLAS Level 1 Givens rotation parameter generation.
      Updates scalar complex128 input a and writes real float64 c plus complex128 s.
    for:
      - zrotg
    labels:
      - rotg
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: srotm
    description: |
      Single-precision BLAS Level 1 modified Givens rotation.
      Applies param-controlled modified rotation to strided float32 vectors x and y.
    for:
      - srotm
    labels:
      - rotm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: drotm
    description: |
      Double-precision BLAS Level 1 modified Givens rotation.
      Applies param-controlled modified rotation to strided float64 vectors x and y.
    for:
      - drotm
    labels:
      - rotm
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: srotmg
    description: |
      Single-precision BLAS Level 1 modified Givens rotation parameter generation.
      Updates scalar float32 d1, d2, x1 and writes the length-5 float32 param vector.
    for:
      - srotmg
    labels:
      - rotmg
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: drotmg
    description: |
      Double-precision BLAS Level 1 modified Givens rotation parameter generation.
      Updates scalar float64 d1, d2, x1 and writes the length-5 float64 param vector.
    for:
      - drotmg
    labels:
      - rotmg
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: sscal
    description: |
      Single-precision BLAS Level 1 vector scaling.
      Updates a strided float32 vector in place with x = alpha * x.
    for:
      - sscal
    labels:
      - scal
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dscal
    description: |
      Double-precision BLAS Level 1 vector scaling.
      Updates a strided float64 vector in place with x = alpha * x.
    for:
      - dscal
    labels:
      - scal
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: cscal
    description: |
      Single-precision complex BLAS Level 1 vector scaling.
      Updates a strided complex64 vector in place with x = alpha * x using a complex scalar.
    for:
      - cscal
    labels:
      - scal
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zscal
    description: |
      Double-precision complex BLAS Level 1 vector scaling.
      Updates a strided complex128 vector in place with x = alpha * x using a complex scalar.
    for:
      - zscal
    labels:
      - scal
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: csscal
    description: |
      Single-precision complex BLAS Level 1 vector scaling by a real scalar.
      Updates a strided complex64 vector in place with x = alpha * x using a float32 scalar.
    for:
      - csscal
    labels:
      - scal
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zdscal
    description: |
      Double-precision complex BLAS Level 1 vector scaling by a real scalar.
      Updates a strided complex128 vector in place with x = alpha * x using a float64 scalar.
    for:
      - zdscal
    labels:
      - scal
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: sgemv
    description: |
      Single-precision BLAS Level 2 general matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for a float32 dense matrix A.
    for:
      - sgemv
    labels:
      - gemv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: sger
    description: |
      Single-precision BLAS Level 2 general rank-1 update.
      Updates A in place with A = alpha * x * y^T + A for a float32 dense matrix A.
    for:
      - sger
    labels:
      - ger
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dger
    description: |
      Double-precision BLAS Level 2 general rank-1 update.
      Updates A in place with A = alpha * x * y^T + A for a float64 dense matrix A.
    for:
      - dger
    labels:
      - ger
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: cgeru
    description: |
      Single-precision complex BLAS Level 2 unconjugated general rank-1 update.
      Updates A in place with A = alpha * x * y^T + A for a complex64 dense matrix A.
    for:
      - cgeru
    labels:
      - ger
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: cgerc
    description: |
      Single-precision complex BLAS Level 2 conjugated general rank-1 update.
      Updates A in place with A = alpha * x * conj(y)^T + A for a complex64 dense matrix A.
    for:
      - cgerc
    labels:
      - ger
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zgeru
    description: |
      Double-precision complex BLAS Level 2 unconjugated general rank-1 update.
      Updates A in place with A = alpha * x * y^T + A for a complex128 dense matrix A.
    for:
      - zgeru
    labels:
      - ger
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zgerc
    description: |
      Double-precision complex BLAS Level 2 conjugated general rank-1 update.
      Updates A in place with A = alpha * x * conj(y)^T + A for a complex128 dense matrix A.
    for:
      - zgerc
    labels:
      - ger
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dgemv
    description: |
      Double-precision BLAS Level 2 general matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for a float64 dense matrix A.
    for:
      - dgemv
    labels:
      - gemv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: cgemv
    description: |
      Single-precision complex BLAS Level 2 general matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for a complex64 dense matrix A.
    for:
      - cgemv
    labels:
      - gemv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: zgemv
    description: |
      Double-precision complex BLAS Level 2 general matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for a complex128 dense matrix A.
    for:
      - zgemv
    labels:
      - gemv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: hgemv
    description: |
      Half-precision BLAS Level 2 general matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for an fp16 dense matrix A.
    for:
      - hgemv
    labels:
      - gemv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: bfgemv
    description: |
      BF16 BLAS Level 2 general matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for a bfloat16 dense matrix A.
    for:
      - bfgemv
    labels:
      - gemv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: fp8_gemv
    description: |
      FP8 BLAS Level 2 general matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for an E4M3/E5M2 fp8 dense matrix A.
    for:
      - fp8_gemv
    labels:
      - gemv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: sgbmv
    description: |
      Single-precision BLAS Level 2 general banded matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for a float32 banded matrix A in BLAS banded storage.
    for:
      - sgbmv
    labels:
      - gbmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: dgbmv
    description: |
      Double-precision BLAS Level 2 general banded matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for a float64 banded matrix A in BLAS banded storage.
    for:
      - dgbmv
    labels:
      - gbmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: cgbmv
    description: |
      Single-precision complex BLAS Level 2 general banded matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for a complex64 banded matrix A in BLAS banded storage.
    for:
      - cgbmv
    labels:
      - gbmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: zgbmv
    description: |
      Double-precision complex BLAS Level 2 general banded matrix-vector multiplication.
      Computes y = alpha * op(A) * x + beta * y for a complex128 banded matrix A in BLAS banded storage.
    for:
      - zgbmv
    labels:
      - gbmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: ssymv
    description: |
      Single-precision BLAS Level 2 symmetric matrix-vector multiplication.
      Computes y = alpha * A * x + beta * y for a float32 symmetric matrix A referenced by its upper or lower triangle.
    for:
      - ssymv
    labels:
      - symv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: dsymv
    description: |
      Double-precision BLAS Level 2 symmetric matrix-vector multiplication.
      Computes y = alpha * A * x + beta * y for a float64 symmetric matrix A referenced by its upper or lower triangle.
    for:
      - dsymv
    labels:
      - symv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: csymv
    description: |
      Single-precision complex BLAS Level 2 symmetric matrix-vector multiplication.
      Computes y = alpha * A * x + beta * y for a complex64 symmetric matrix A referenced by its upper or lower triangle.
    for:
      - csymv
    labels:
      - symv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: zsymv
    description: |
      Double-precision complex BLAS Level 2 symmetric matrix-vector multiplication.
      Computes y = alpha * A * x + beta * y for a complex128 symmetric matrix A referenced by its upper or lower triangle.
    for:
      - zsymv
    labels:
      - symv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: sspr
    description: |
      Single-precision BLAS Level 2 symmetric packed rank-1 update.
      Updates a float32 symmetric matrix in BLAS packed storage with
      AP = alpha * x * x^T + AP.
    for:
      - sspr
    labels:
      - spr
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dspr
    description: |
      Double-precision BLAS Level 2 symmetric packed rank-1 update.
      Updates a float64 symmetric matrix in BLAS packed storage with
      AP = alpha * x * x^T + AP.
    for:
      - dspr
    labels:
      - spr
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: sspr2
    description: |
      Single-precision BLAS Level 2 symmetric packed rank-2 update.
      Updates a float32 symmetric matrix in BLAS packed storage with
      AP = alpha * x * y^T + alpha * y * x^T + AP.
    for:
      - sspr2
    labels:
      - spr2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dspr2
    description: |
      Double-precision BLAS Level 2 symmetric packed rank-2 update.
      Updates a float64 symmetric matrix in BLAS packed storage with
      AP = alpha * x * y^T + alpha * y * x^T + AP.
    for:
      - dspr2
    labels:
      - spr2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: ssyr2
    description: |
      Single-precision BLAS Level 2 symmetric rank-2 update.
      Updates the referenced triangle of a float32 symmetric matrix with
      A = alpha * x * y^T + alpha * y * x^T + A.
    for:
      - ssyr2
    labels:
      - syr2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dsyr2
    description: |
      Double-precision BLAS Level 2 symmetric rank-2 update.
      Updates the referenced triangle of a float64 symmetric matrix with
      A = alpha * x * y^T + alpha * y * x^T + A.
    for:
      - dsyr2
    labels:
      - syr2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  # csyr2/zsyr2 are disabled in the strict workflow: SciPy/CPU BLAS does not
  # expose direct complex SYR2 references, so they cannot meet CPU correctness.
  # - id: csyr2
  #   description: |
  #     Single-precision complex BLAS Level 2 symmetric rank-2 update.
  #     Updates the referenced triangle of a complex64 symmetric matrix with
  #     A = alpha * x * y^T + alpha * y * x^T + A.
  #   for:
  #     - csyr2
  #   labels:
  #     - syr2
  #   kind:
  #     - BLAS
  #   stages:
  #     - beta: '0.1'
  # - id: zsyr2
  #   description: |
  #     Double-precision complex BLAS Level 2 symmetric rank-2 update.
  #     Updates the referenced triangle of a complex128 symmetric matrix with
  #     A = alpha * x * y^T + alpha * y * x^T + A.
  #   for:
  #     - zsyr2
  #   labels:
  #     - syr2
  #   kind:
  #     - BLAS
  #   stages:
  #     - beta: '0.1'
  - id: cher2
    description: |
      Single-precision complex BLAS Level 2 Hermitian rank-2 update.
      Updates the referenced triangle of a complex64 Hermitian matrix with
      A = alpha * x * y^H + conj(alpha) * y * x^H + A.
    for:
      - cher2
    labels:
      - her2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zher2
    description: |
      Double-precision complex BLAS Level 2 Hermitian rank-2 update.
      Updates the referenced triangle of a complex128 Hermitian matrix with
      A = alpha * x * y^H + conj(alpha) * y * x^H + A.
    for:
      - zher2
    labels:
      - her2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: chemv
    description: |
      Single-precision complex BLAS Level 2 Hermitian matrix-vector multiplication.
      Computes y = alpha * A * x + beta * y for a complex64 Hermitian matrix A referenced by its upper or lower triangle.
    for:
      - chemv
    labels:
      - hemv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: zhemv
    description: |
      Double-precision complex BLAS Level 2 Hermitian matrix-vector multiplication.
      Computes y = alpha * A * x + beta * y for a complex128 Hermitian matrix A referenced by its upper or lower triangle.
    for:
      - zhemv
    labels:
      - hemv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: strmv
    description: |
      Single-precision BLAS Level 2 triangular matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a float32 triangular matrix A.
    for:
      - strmv
    labels:
      - trmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: dtrmv
    description: |
      Double-precision BLAS Level 2 triangular matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a float64 triangular matrix A.
    for:
      - dtrmv
    labels:
      - trmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: ctrmv
    description: |
      Single-precision complex BLAS Level 2 triangular matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a complex64 triangular matrix A.
    for:
      - ctrmv
    labels:
      - trmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: ztrmv
    description: |
      Double-precision complex BLAS Level 2 triangular matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a complex128 triangular matrix A.
    for:
      - ztrmv
    labels:
      - trmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: stbmv
    description: |
      Single-precision BLAS Level 2 triangular banded matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a float32 triangular banded matrix A in BLAS banded storage.
    for:
      - stbmv
    labels:
      - tbmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: dtbmv
    description: |
      Double-precision BLAS Level 2 triangular banded matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a float64 triangular banded matrix A in BLAS banded storage.
    for:
      - dtbmv
    labels:
      - tbmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: ctbmv
    description: |
      Single-precision complex BLAS Level 2 triangular banded matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a complex64 triangular banded matrix A in BLAS banded storage.
    for:
      - ctbmv
    labels:
      - tbmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: ztbmv
    description: |
      Double-precision complex BLAS Level 2 triangular banded matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a complex128 triangular banded matrix A in BLAS banded storage.
    for:
      - ztbmv
    labels:
      - tbmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: stpmv
    description: |
      Single-precision BLAS Level 2 triangular packed matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a float32 triangular matrix A in BLAS packed storage.
    for:
      - stpmv
    labels:
      - tpmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: dtpmv
    description: |
      Double-precision BLAS Level 2 triangular packed matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a float64 triangular matrix A in BLAS packed storage.
    for:
      - dtpmv
    labels:
      - tpmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: ctpmv
    description: |
      Single-precision complex BLAS Level 2 triangular packed matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a complex64 triangular matrix A in BLAS packed storage.
    for:
      - ctpmv
    labels:
      - tpmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: ztpmv
    description: |
      Double-precision complex BLAS Level 2 triangular packed matrix-vector multiplication.
      Updates x in place with x = op(A) * x for a complex128 triangular matrix A in BLAS packed storage.
    for:
      - ztpmv
    labels:
      - tpmv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: stbsv
    description: |
      Single-precision BLAS Level 2 triangular banded solve.
      Solves op(A) * x = b in place for a float32 triangular banded matrix A in BLAS banded storage.
    for:
      - stbsv
    labels:
      - tbsv
    kind:
      - BLAS
    stages:
      - stable: '0.1'
  - id: chpmv
    description: |
      Single-precision complex BLAS Level 2 Hermitian packed matrix-vector multiplication.
      Computes y = alpha * A * x + beta * y for a complex64 Hermitian matrix A in BLAS packed storage.
    for:
      - chpmv
    labels:
      - hpmv
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zhpmv
    description: |
      Double-precision complex BLAS Level 2 Hermitian packed matrix-vector multiplication.
      Computes y = alpha * A * x + beta * y for a complex128 Hermitian matrix A in BLAS packed storage.
    for:
      - zhpmv
    labels:
      - hpmv
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: chpr
    description: |
      Single-precision complex BLAS Level 2 Hermitian packed rank-1 update.
      Updates a complex64 Hermitian matrix in packed storage with
      AP = alpha * x * x^H + AP, where alpha is real.
    for:
      - chpr
    labels:
      - hpr
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zhpr
    description: |
      Double-precision complex BLAS Level 2 Hermitian packed rank-1 update.
      Updates a complex128 Hermitian matrix in packed storage with
      AP = alpha * x * x^H + AP, where alpha is real.
    for:
      - zhpr
    labels:
      - hpr
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: chpr2
    description: |
      Single-precision complex BLAS Level 2 Hermitian packed rank-2 update.
      Updates a complex64 Hermitian matrix in packed storage with
      AP = alpha * x * y^H + conj(alpha) * y * x^H + AP.
    for:
      - chpr2
    labels:
      - hpr2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: zhpr2
    description: |
      Double-precision complex BLAS Level 2 Hermitian packed rank-2 update.
      Updates a complex128 Hermitian matrix in packed storage with
      AP = alpha * x * y^H + conj(alpha) * y * x^H + AP.
    for:
      - zhpr2
    labels:
      - hpr2
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: strsv
    description: |
      Single-precision BLAS Level 2 triangular solve.
      Solves op(A) * x = b in place for a float32 triangular matrix A.
    for:
      - strsv
    labels:
      - trsv
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: dtrsv
    description: |
      Double-precision BLAS Level 2 triangular solve.
      Solves op(A) * x = b in place for a float64 triangular matrix A.
    for:
      - dtrsv
    labels:
      - trsv
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: ctrsv
    description: |
      Single-precision complex BLAS Level 2 triangular solve.
      Solves op(A) * x = b in place for a complex64 triangular matrix A.
    for:
      - ctrsv
    labels:
      - trsv
    kind:
      - BLAS
    stages:
      - beta: '0.1'
  - id: ztrsv
    description: |
      Double-precision complex BLAS Level 2 triangular solve.
      Solves op(A) * x = b in place for a complex128 triangular matrix A.
    for:
      - ztrsv
    labels:
      - trsv
    kind:
      - BLAS
    stages:
      - beta: '0.1'
